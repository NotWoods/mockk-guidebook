const accessKeys=new Map,focusable=document.querySelectorAll('input[accesskey]');focusable.forEach(e=>{accessKeys.set(e.accessKey,e)}),document.addEventListener('keydown',e=>{if(e.repeat)return;const t=accessKeys.get(e.key);t&&(e.preventDefault(),t.click())})