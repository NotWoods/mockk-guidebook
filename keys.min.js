const accessKeys=new Map();const focusable=document.querySelectorAll('input[accesskey]');focusable.forEach((input)=>{accessKeys.set(input.accessKey,input);});document.addEventListener('keydown',(evt)=>{if(evt.repeat)return;const input=accessKeys.get(evt.key);if(input){evt.preventDefault();input.click();}});