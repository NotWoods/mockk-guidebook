const accessKeys=new Map,focusable=document.querySelectorAll('input[accesskey]');focusable.forEach(a=>{accessKeys.set(a.accessKey,a)}),document.addEventListener('keydown',a=>{if(a.repeat)return;const b=accessKeys.get(a.key);b&&(a.preventDefault(),b.click())})